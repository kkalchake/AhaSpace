spring:
  application:
    name: Enlightenment
  datasource:
    url: jdbc:h2:mem:learning_db
    driver-class-name: org.h2.Driver
    username:
    password:
  h2:
    console:
      enabled: true
      path: /h2-console